services:
  octopus-mma:
    build: .
    container_name: octopus_mma
    restart: always
    environment:
      - EDITOR_PASSWORD=${EDITOR_PASSWORD}
    networks:
      - web_proxy

networks:
  web_proxy:
    external: true
